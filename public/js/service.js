window.addEventListener('load', () => {
        // Ensure the layout is ready before showing animations
        const hero = document.querySelector('.dental-hero');
        hero.style.visibility = 'visible';
    });